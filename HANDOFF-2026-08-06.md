# Hand-off Note â€” 2026-08-06 (AEST)

**From**: Danish's laptop (Cursor session f0f4de24-953c-4fa2-97d7-b0aa25354816)
**To**: New laptop / next Cursor session
**Project**: Bitbro4crypto Conservative BTC bot (doxed-founders-website)
**Author**: Cursor agent
**Status at handoff**: Paper soak running, all systems green, awaiting 48h of paper data before any live decision.

---

## 1. PROJECT CONTEXT â€” What is this

A BTC perpetual trading bot (`services/btc-conservative-agent/bot.py`) that:
- Connects to Bitfinex WS for live market data (`tBTCF0:USTF0` perpetual)
- Calls DeepSeek AI every ~3 min to classify direction (LONG vs SHORT)
- Runs a 2-lane paper research stack: CONTINUOUS (benchmark) + TYPE_B_HUNTER_V1 (candidate)
- Places virtual limit/chase orders in paper mode (no real Bitfinex orders)
- Mirrors lifecycle events to Railway (relay executor), Vercel (Agent Hub frontend), Neon (Postgres)

**Architecture (canonical owner = Fly.io)**:
```
Fly.io bot â”€â”€â–º DeepSeek AI (every 3 min)
   â”‚      â””â”€â–º Bitfinex WS (live ticks)
   â”‚
   â”œâ”€â”€â–º Railway (relay executor + NestJS API) â”€â”€â–º Neon (Postgres)
   â”‚
   â”œâ”€â”€â–º Vercel (Next.js Agent Hub at doxxedcrypto.digital)
   â”‚
   â””â”€â”€â–º Local laptop (analyzer dashboard + sync loop, read-only)
```

**Key principle**: Fly.io is canonical. Local laptop is read-only mirror + research analyzer. Local must never make trading decisions.

---

## 2. CURRENT WORKSPACE PATH

**Canonical git repo**: `C:\Users\user\Desktop\Final Bots\doxedcryptofounder-recovered`
- `origin` = `https://github.com/danishhaiderau-maker/doxed-founders-website.git`
- HEAD = `f03640dd` (Stage 1 strategy fixes â€” see Â§3)

**Stale copy (DO NOT USE)**: `C:\Users\user\Desktop\Final Bots\doxedcryptofounder`
- This is a dead remnant. Only contains dashboard.html, scan-creds.ps1, _cleanup_scan*.ps1.
- No `services/` folder, no `bot.py`. Cursor's local-history `.git` remnant.
- Was supposed to be renamed to `.stale-backup-20260806` but **rename was blocked by Cursor workspace lock** (Cursor had the folder open).
- **On new laptop**: only copy `doxedcryptofounder-recovered`. Don't bother with the stale folder.

---

## 3. STAGE 1 STRATEGY FIXES â€” Shipped today

**Commit**: `f03640dd fix(strategy): Stage 1 â€” restore AI gap policy, kill regime anchor bias, harden risk gates`
- Pushed to `origin/master`
- Deployed on Fly (verified via `/health` â†’ `git_rev: "f03640ddc01c"`)
- [+479 / âˆ’72 across 3 files](f03640dd#changes): `bot.py`, `test_continuous_r2_floor.py`, `test_strategy_fixes_stage1.py`
- All 72 tests pass

### Root causes that Stage 1 fixed

Diagnosed from prior session data (1,562 AI calls analyzed):

| # | Bug | Effect | Evidence |
|---|---|---|---|
| 1 | DeepSeek returned `long_score=0, short_score=0` on 100% of calls | Gap policy never gated anything; direction from AI's text field | ai_input_log.jsonl, all 1,562 rows |
| 2 | 9+ redundant BULL regime labels in AI context | AI anchored on regime, ignored market_structure_shift, picked LONG against BEAR_CONTINUATION 41% of time | enrich_ai_context_upgrade() audit |
| 3 | No structure-agreement gate | Wrong-direction trades passed | 14 of last 20 LONG picks were against BEAR_CONTINUATION |
| 4 | `CONTINUOUS_MIN_SPREAD_FLOOR = 4` had `* 10` multiplier | Effective threshold was raw gap â‰¥40 (bucket 4) instead of â‰¥5; lane was starved | Code review at spawn_continuous_lane_from_ai_scan |
| 5 | `THESIS_MFE_PROTECT_PCT = 2.0` | +2% MFE was chop noise; fast-cut skipped; losers ran from âˆ’12% to âˆ’30% SL | 3 recent losers each touched +2% then bled to âˆ’$6 |
| 6 | `/api/state` `ai_input` silently fell back to `feature_snapshot` (6-key buffer) when no AI call yet | Misled Danish into thinking AI features were "stripped" | Direct probe of /api/state |

### What Stage 1 changed (all in `bot.py` unless noted)

| Fix | Location | Change |
|---|---|---|
| #1 zero-score reject | `_apply_zero_score_reject_check` @ line 11078, wired @ 10867 & 10916 | Reject AI responses where `long_score + short_score < 50` with reason `AI_RETURNED_ZERO_SCORES`. Forces AI to commit, restores gap policy. |
| #2 drop regime labels | `enrich_ai_context_upgrade` @ 10537-10636, prompt @ 22459 | Removed `regime_ema200`, `regime_trend_health`, `last_3_regimes`, `last_3_signals`, `trend_state_last_3_cycles`, `market_regime_tracker`, `trend_health_detail.base_state`. Kept only raw `regime`. Added prompt instruction: *"Do not anchor on the regime label â€” derive direction from structure, order flow, and EMA alignment in that order."* |
| #3 structure gate | New `apply_structure_agreement_gate` helper @ 11236, wired @ 15462 | Hard reject `AI_LONG_VS_BEAR_STRUCTURE` / `AI_SHORT_VS_BULL_STRUCTURE`. |
| #4 remove `* 10` scaling | `CONTINUOUS_MIN_SPREAD_FLOOR` @ 6554, spawn check @ 14833 | Effective threshold now raw gap â‰¥4 (was â‰¥40). **Note: this reverses a prior commit `dc73fc6f` that had backtest evidence for â‰¥40. Paper soak will decide if it stays.** |
| #5 MFE-protect 2â†’5 | `THESIS_MFE_PROTECT_PCT` @ 6348 | Tighten MFE-protect so fast-cut fires instead of SL |
| #6 honest fallback | `_build_api_state_snapshot` @ 30337-30347 | `ai_input` returns `{"status": "NO_AI_CALL_YET", ...}` instead of misleading `feature_snapshot` |

### Tests added

`test_strategy_fixes_stage1.py`:
- `test_zero_score_reject()` â€” verifies Fix #1
- `test_structure_gate_rejects_long_vs_bear()` â€” verifies Fix #3
- 10 tests total, all passing

---

## 4. SYSTEM STATUS AT HANDOFF â€” All green

### Fly.io bot (canonical) â€” **HEALTHY, PAPER MODE**

- URL: `https://doxed-btc-bot.fly.dev`
- App name: `doxed-btc-bot`
- Machine: `48e1403c073038` (sin region)
- `git_rev: f03640ddc01c` âœ… (Stage 1 live)
- `force_paper_mode: true` âœ… (safety)
- `live_armed: false` âœ… (relay disarmed)
- `trading_block_reason: FORCE_PAPER_MODE` âœ…
- `system_ready: true` âœ…
- `ws_transport_connected: true`, `market_data_ready: true`
- AI call cadence: ~3 min (healthy)
- Trade count session: 61

**Bot health probe URLs** (use from new laptop to verify):
```
curl https://doxed-btc-bot.fly.dev/health
curl -H "X-Bot-Admin-Token: breFHufoUGTgJh5ANAXI2g8MctjU-PVxDjMb8J7Oc7s" https://doxed-btc-bot.fly.dev/api/state
```

### Auth â€” admin token (rotated today)

Old token `pfrwuIgKX1I-qT20UcvDv4FO0fuSiPWuTXpC5JGxt2M` is **REJECTED** (it's in some old session memory but the deployed secret changed).

**Current deployed token**: `breFHufoUGTgJh5ANAXI2g8MctjU-PVxDjMb8J7Oc7s`

Auth mechanism (from `bot.py:22614-22626`):
- Header: `X-Bot-Admin-Token: <token>`
- Cookie: `bot_admin_token=<token>`
- The `/` route accepts `?admin_token=<token>` and sets the cookie for 30 days

**Dashboard login URL** (bookmark on new laptop):
```
https://doxed-btc-bot.fly.dev/?admin_token=breFHufoUGTgJh5ANAXI2g8MctjU-PVxDjMb8J7Oc7s
```

**Vault file recreated at**: `C:\Users\user\Desktop\Final Bots\doxedcryptofounder-secrets\vault\home-bot.env`
- On new laptop: copy this folder over, or the sync loop won't authenticate.
- Also set as Windows user env var: `BOT_ADMIN_TOKEN`

### Railway (relay executor + NestJS API) â€” **HEALTHY**

- API URL: `https://doxed-founders-website-production.up.railway.app`
- `/api/health` â†’ 200 OK `{"status":"ok","services":{"api":"ok","database":"ok"}}`
- No code changes needed from Stage 1 PR (railway hosts the executor, not strategy)
- Showcase routes confirmed working at:
  - `/api/trading-agents/conservative-btc` â†’ 200 (bot present, `botConnected:true`, `flyReachable:true`)
  - `/api/trading-agents/conservative-btc/dashboard` â†’ 200
- Earlier confusion: `/api/trading-agents/showcase` and `/by-slug/...` return 404 â€” **those routes never existed**. Use the routes above.

### Vercel (Agent Hub) â€” **HEALTHY**

- URL: `https://doxxedcrypto.digital/`
- Agent Hub: `https://doxxedcrypto.digital/agent-hub/conservative-btc`
- Latest production deployment is current
- No changes needed from Stage 1 PR (doesn't touch `apps/web`)
- If showcase looks blank in browser: **hard refresh (`Ctrl+Shift+R`)** or check browser console for client-side errors. Backend is verified returning the bot data.

### Neon (Postgres) â€” **CONNECTED**

- Reachable via Railway API and Vercel health checks (`database: ok`)
- No schema migration needed for Stage 1

---

## 5. LOCAL PROCESSES ON DANISH'S LAPTOP

### Research analyzer â€” RUNNING

- URL: `http://127.0.0.1:9001/`
- PIDs (on this laptop): 18408 (Flask), 24512 (analysis engine)
- Launched via `start-home-analyzer.ps1 -Port 9001 -NoWait`
- Script: `services/btc-conservative-agent/research_dashboard.py` (run with `--standalone`)
- Auto-refreshes every 3 min from `fly-data-mirror/` folder

### Flyâ†’local sync loop â€” RUNNING

- PID: 1212 (launched via wscript.exe scripts\start-sync-loop-hidden.vbs — runs truly hidden, no PowerShell/conhost window flash)
- Script: `sync-fly-bot-data-loop.ps1` (in `scripts/` folder)
- Heartbeat file: `.fly-data-sync-loop.heartbeat.json`
- Sync interval: ~3 min
- Two functions:
  1. Mirror Fly CSV/JSONL exports â†’ local `services/btc-conservative-agent/fly-data-mirror/`
  2. **Prune Fly `/app/data` volume when >80% full** (critical â€” prevents disk-full bot hang)

### On new laptop â€” you'll need to:

1. Copy `doxedcryptofounder-recovered/` (the git repo, ~500MB without node_modules)
2. Copy `doxedcryptofounder-secrets/` (the vault â€” small but critical)
3. Run `npm install` in repo root + `apps/api` + `apps/web` to rebuild node_modules
4. Install Python deps: `pip install -r services/btc-conservative-agent/requirements.txt` (or equivalent)
5. Install flyctl and authenticate: `flyctl auth login`
6. Set env vars (or rely on vault file):
   - `BOT_ADMIN_TOKEN=breFHufoUGTgJh5ANAXI2g8MctjU-PVxDjMb8J7Oc7s`
   - `BTC_AGENT_DATA_DIR=<repo>/services/btc-conservative-agent/fly-data-mirror`
   - `BTC_AGENT_REPORT_DIR=<repo>/services/btc-conservative-agent/fly-data-mirror`
7. Launch the analyzer:
   ```powershell
   cd "C:\Users\user\Desktop\Final Bots\doxedcryptofounder-recovered\services\btc-conservative-agent"
   python research_dashboard.py --standalone
   ```
8. Launch the sync loop:
```powershell
cd "C:\Users\user\Desktop\Final Bots\doxedcryptofounder-recovered"
# Silent (recommended) - no PowerShell/conhost window flash:
wscript.exe scripts\start-sync-loop-hidden.vbs

# Legacy (visible console window each cycle - NOT recommended):
# powershell -ExecutionPolicy Bypass -File scripts\sync-fly-bot-data-loop.ps1
```

---

## 6. PAPER SOAK STATUS

**Started**: 2026-08-06 ~13:45 AEST (after Stage 1 deployed + bot recovered from disk-full hang)
**Target duration**: 48 hours
**Ends**: ~2026-08-08 13:45 AEST

### What to watch during paper soak

1. **Direction picks vs `market_structure_shift`** â€” pre-Stage-1 disagreement was 41%. Target: under 15%. Fixes #2 + #3 should drive this.
2. **Exit distribution** â€” losing trades should cluster at **âˆ’12%** (`THESIS_FAST_CUT`) instead of **âˆ’30%** (`STOP_LOSS`). Fix #5 should drive this.
3. **Continuous lane spawn frequency** â€” should increase vs pre-fix. Fix #4 should drive this.
4. **Zero-score reject count** â€” if DeepSeek is still returning zeros, you'll see `AI_RETURNED_ZERO_SCORES` in logs. If scores are real (recent observation: long=45, short=55), Fix #1 is working as designed.
5. **Cumulative PnL** â€” currently **âˆ’$20.38** (60 trades). This is pre-Stage-1 cumulative. Will improve as post-Stage-1 trades dilute the old losses. **Don't judge the strategy by today's cumulative.**

### Early positive signal

First post-Stage-1 trade at 2026-08-06 13:45 (`cont-a53ea2036acb`):
- Direction: **SHORT** âœ… (correct for current regime â€” pre-Stage-1 would have picked LONG)
- PnL: **+10.18%** = **+$2.04**
- Duration: 43.1 min

One trade is anecdotal, but it's the right pattern. 5 consecutive SHORT AI calls after restart (all APPROVE, all aligned with bearish structure) â€” exactly what we wanted.

---

## 7. KNOWN OPERATIONAL ISSUES (non-blocking)

### 7.1 Fly `/app/data` volume fills in 3-4 days

- Volume size: 1 GB
- Was 100% full today (caused bot hang). Worker freed ~600 MB by deleting 24 rotated `signal_replay.jsonl.N` files.
- Sync loop now auto-prunes when >80% full.
- **Permanent fix options** (defer until after paper soak):
  - Bump Fly volume from 1 GB â†’ 2 GB (`flyctl volumes ...`)
  - Tighten log retention policy in bot.py (fewer rotated files kept)

### 7.2 WS freshness gate flaps on sparse tape

- `WS_ENTRY_FRESH_SEC = 60` (env var) â€” bot blocks new entries when no trade tick in 60s.
- Bitfinex `tBTCF0:USTF0` perpetual tape sometimes goes quiet >60s.
- Symptom: `execution_reason` cycles `ALLOWED â†” BLOCKED`.
- Not a bug â€” by design to prevent trading on stale prices.
- **Mitigation options** (defer):
  - Raise `WS_ENTRY_FRESH_SEC` from 60 â†’ 120 (Fly env var)
  - Add redundant data source (spot `tBTCUSD`)

### 7.3 Dashboard `DeepSeek MISSING` is a stale-render lie

- The dashboard JS sometimes shows `DeepSeek key MISSING (.env)` even when key is set.
- Verified: `flyctl secrets list` shows `DEEPSEEK_API_KEY: Deployed`.
- `printenv DEEPSEEK_API_KEY` on Fly VM returned `sk-95bb2523...` (33 chars).
- This is a dashboard display bug, not a real issue. **Hard refresh (`Ctrl+Shift+R`) clears it.**

### 7.4 Cursor folder lock blocking rename

- Cannot rename `doxedcryptofounder` â†’ `.stale-backup-20260806` because Cursor has it open as workspace root.
- Doesn't affect anything functional â€” all workers operate from `doxedcryptofounder-recovered`.
- **On new laptop**: just open `doxedcryptofounder-recovered` directly. Skip the stale folder entirely.

### 7.5 Orphaned PID 5032 on Danish's laptop

- Python process from Aug 3, no command line, access-denied when killed.
- Not listening on 7002/9001 or any port we care about.
- Kill from admin PowerShell if it bothers you: `Start-Process powershell -Verb RunAs -ArgumentList 'Stop-Process -Id 5032 -Force'`

---

## 8. AGENT HUB DASHBOARD (doxxedcrypto.digital)

### How to view the bot in Agent Hub

- URL: `https://doxxedcrypto.digital/agent-hub/conservative-btc`
- Backend verified returning bot data (see Â§4 Railway section)
- If page looks blank: **browser-side issue**. Hard refresh, clear cache, check console (F12), disable adblocker.

### Gap/chase bucket toggles

- **Location**: ON THE BOT DASHBOARD (`https://doxed-btc-bot.fly.dev/`), not Agent Hub
- **Audit result**: fully wired UIâ†’APIâ†’botâ†’persistence (verified by [Toggle UI audit](f90ea873-9392-46fb-8801-51523f3c2cdb))
- **Persistence**: `config-7002.json` on Fly volume (rehydrated on restart)
- **Auth required**: must be authenticated (cookie set via `/?admin_token=...`) to save toggles
- **Agent Hub does NOT mirror these toggles** â€” only the bot dashboard has them. Building an Agent Hub mirror is a possible future housekeeping PR.

---

## 9. PENDING / DEFERRED TASKS

### Triggered by paper soak results (T+48h)

- **If raw-gap 5-39 trades lose**: revert just Fix #4 (restore `* 10` scaling). Other fixes stay.
- **If AI still fights structure after Fix #2 + #3**: tighten structure-agreement thresholds.
- **If cumulative PnL trending positive**: proceed to Stage F â€” re-arm relay for one guarded live Bitfinex cycle.

### Housekeeping (low priority)

- **H1**: Remove "Showcase bot feed" panel from dashboard
- **H2**: Remove "Performance Â· 30D" chart
- **H3**: Investigate Bitfinex live-copy vs relay layout discrepancy
- **H4**: Investigate "hidden order" on Bitfinex mobile (was paper, not real)
- **M4**: Railway runtime audit â€” strip research-only storage
- **M5**: Vercel visual QA sweep
- **L1**: Tile 2 funnel cluster removal
- **L2**: R2 backtest re-run
- **L3**: Rotate exposed secrets (user task)
- **L4**: Voice hotfix removal
- **L7**: Clean up unstaged modified files

### Feature requests (Danish)

- **AI reply text on dashboard** â€” currently `ai_reason_research.jsonl` has the data, but the dashboard's "AI History (Session)" table only shows verdicts, not raw AI text/reason. Build a UI feature to surface the actual AI response (direction + scores + reason). Scope after paper soak.
- **Agent Hub mirror of gap/chase toggles** â€” convenience UI. CORS-ready `/api/spread-gate` endpoint already exists on bot for this purpose. Not required.
- **Demo Mode** â€” Danish dropped this earlier. May revisit.

---

## 10. KEY FILES & PATHS QUICK REFERENCE

### Source code (in `doxedcryptofounder-recovered/`)
- `services/btc-conservative-agent/bot.py` â€” the bot (35,268 lines in deployed Docker image; ~33,159 locally due to Dockerfile wrappers)
- `services/btc-conservative-agent/research_dashboard.py` â€” local analyzer script
- `services/btc-conservative-agent/fly.toml` â€” Fly deploy config
- `apps/api/src/trading-agents/` â€” NestJS API (Railway)
- `apps/web/src/components/agent-hub/` â€” Next.js Agent Hub (Vercel)
- `packages/utils/src/melbourne-time.ts` â€” timezone utilities
- `config/fly-canonical.lock.json` â€” Fly canonical owner enforcement

### Config / state
- `config-7002.json` (on Fly volume at `/app/data/config-7002.json`) â€” bot dashboard config, persisted across restarts
- `services/btc-conservative-agent/fly-data-mirror/` â€” local mirror of Fly exports
- `C:\Users\user\Desktop\Final Bots\doxedcryptofounder-secrets\vault\home-bot.env` â€” admin token vault

### Tests
- `services/btc-conservative-agent/test_strategy_fixes_stage1.py` â€” Stage 1 coverage
- `services/btc-conservative-agent/test_type_b_spread_floor.py`
- `services/btc-conservative-agent/test_continuous_r2_floor.py`
- `services/btc-conservative-agent/test_ws_live_readiness_contract.py`

### Run tests
```powershell
cd "C:\Users\user\Desktop\Final Bots\doxedcryptofounder-recovered\services\btc-conservative-agent"
python -m pytest test_strategy_fixes_stage1.py test_type_b_spread_floor.py test_continuous_r2_floor.py test_ws_live_readiness_contract.py -v
```

---

## 11. KEY COMMANDS QUICK REFERENCE

### Verify Fly bot health
```powershell
(Invoke-WebRequest "https://doxed-btc-bot.fly.dev/health" -UseBasicParsing).Content | ConvertFrom-Json | ConvertTo-Json -Depth 6
```

### Verify Fly bot state (with admin)
```powershell
$h = @{ "X-Bot-Admin-Token" = "breFHufoUGTgJh5ANAXI2g8MctjU-PVxDjMb8J7Oc7s" }
(Invoke-WebRequest "https://doxed-btc-bot.fly.dev/api/state" -Headers $h -UseBasicParsing).Content | ConvertFrom-Json | ConvertTo-Json -Depth 6
```

### SSH into Fly
```powershell
flyctl ssh console -a doxed-btc-bot
```
(Inside the SSH session: avoid `head`, `tail`, `;`, `|` in commands â€” PowerShell quoting mangles them across SSH. Use `awk`, `sed -n`, `grep` instead.)

### Read Fly bot logs
```powershell
flyctl ssh console -a doxed-btc-bot --command "awk 'END{print NR}' /app/data/runtime/ai_reason_research.jsonl"
flyctl ssh console -a doxed-btc-bot --command "sed -n '$(( $(awk 'END{print NR}' /app/data/runtime/ai_reason_research.jsonl) - 2 )),\$p' /app/data/runtime/ai_reason_research.jsonl"
```

### Restart Fly bot
```powershell
flyctl apps restart doxed-btc-bot
```
(Always verify `force_paper_mode=true` and `live_armed=false` after restart.)

### Set a Fly secret
```powershell
flyctl secrets set KEY=value -a doxed-btc-bot
```
(Triggers rolling restart.)

### Check Railway
```powershell
(Invoke-WebRequest "https://doxed-founders-website-production.up.railway.app/api/health" -UseBasicParsing).Content
```

---

## 12. SAFETY CONTRACT â€” Read before any live action

Before any future "go live" with real Bitfinex orders, ALL of these must be true:

1. âœ… Paper soak has run 48h with positive trajectory
2. âœ… Losing trades exit at âˆ’12% (THESIS_FAST_CUT), not âˆ’30% (STOP_LOSS)
3. âœ… AI direction picks align with `market_structure_shift` >85% of time
4. âœ… Zero-score reject is NOT firing on most calls (DeepSeek returning real scores)
5. âœ… `force_paper_mode` is intentionally disabled (requires Danish's explicit approval)
6. âœ… Relay is armed via authenticated Agent Hub click (only Danish can do this)
7. âœ… `BOT_ADMIN_TOKEN` and `BOT_CONTROL_SECRET` are rotated and current on Fly + Railway

**Until all 7 are true: paper only. No exceptions.**

---

## 13. WHAT TO DO FIRST ON THE NEW LAPTOP

1. Copy these two folders from old laptop (or re-clone from GitHub):
   - `doxedcryptofounder-recovered/` (the repo)
   - `doxedcryptofounder-secrets/` (the vault)

2. Open Cursor at `doxedcryptofounder-recovered/`

3. Verify Fly bot is still healthy:
   ```powershell
   (Invoke-WebRequest "https://doxed-btc-bot.fly.dev/health" -UseBasicParsing).Content
   ```
   Expect: `git_rev: f03640ddc01c`, `force_paper_mode: true`, `live_armed: false`, `system_ready: true`

4. Open the dashboard (bookmark this):
   ```
   https://doxed-btc-bot.fly.dev/?admin_token=breFHufoUGTgJh5ANAXI2g8MctjU-PVxDjMb8J7Oc7s
   ```

5. Confirm local analyzer + sync loop are running (or relaunch per Â§5)

6. Resume watching paper soak. Don't touch strategy code. Don't re-arm relay.

7. Paper soak ends ~2026-08-08 13:45 AEST. Evaluate then.

---

## 14. CREDITS / AUDIT TRAIL

This hand-off was prepared after a multi-day session that shipped:
- Stage 1 strategy fixes (`f03640dd`) â€” 6 fixes, 72 tests, all green
- BOT_ADMIN_TOKEN rotation end-to-end
- Sync loop restoration
- Multiple worker dispatches verified by independent audit subagents

Key subagent reports (in `agent-transcripts/` folder) for reference:
- [Stage 1 strategy fixes worker](3ec33f85-fe08-4148-b487-b2e4eb812ab6) â€” implemented all 6 fixes
- [Toggle UI audit](f90ea873-9392-46fb-8801-51523f3c2cdb) â€” verified gap/chase toggles fully wired
- [AI bias investigation](f811c590-e8b8-4c38-8747-c09b64ed186e) â€” found zero-score root cause
- [AI feature audit](ff6fcfb2-c6f6-4b5e-a8f1-a204d57a43e3) â€” confirmed features NOT stripped
- [Lane audit](d1ff558e-974d-4ecc-838d-e41446fbfc19) â€” found R2 `* 10` scaling bug
- [Trade audit](dd30b531-007a-493d-bbbb-5f00865fadd8) â€” bucketed the âˆ’$20.38 session loss
- [Deploy worker](c7477e46-ee95-4dfc-b46b-a4a5b41f5bc9) â€” verified Stage 1 live on Fly
- [BLOCKED state fix](4addc345-c3be-4992-a32c-71b265c3b3c7) â€” unblocked bot after WS flap
- [Showcase audit](5a656391-1f28-4288-adb5-cd661932c071) â€” confirmed showcase routes work
- [Dashboard stale data probe](3e4f8094-ba2b-42da-86c5-02b9a39a8d7d) â€” confirmed bot healthy
- [Token rotation worker](5630cdb8-9e1f-45f0-a20d-b95aed7b9cc2) â€” restored sync loop auth
- [Analyzer restart worker](bcbedbdd-0596-4d37-98df-64ed3be4e3b4) â€” restored local analyzer

---

**End of hand-off.**
