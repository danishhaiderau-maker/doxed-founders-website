# Current-generation shadow discovery integration

Source-only integration; no deployment or fresh analyzer publication claimed.

The atomic publisher passes this invocation's shadow result directly to the
discovery adapter. A failed shadow publication supplies an explicit invalid input
instead of reloading a previous report. Same-generation validation and terminal
hashes bind accepted composite entry/exit outcomes to the baseline entry.

Independent review led to exact aggregate UNKNOWN preservation, rejection of
invalid reason counts/blocker types, input baseline/shadow hashes, and explicit
terminal cost/position/coverage signature references. A capped example list never
replaces the full unknown count. Exact duplicate composite outcomes are deduped;
the upstream parameter-replay total remains labeled as such, not independent
trades. Conflicting outcomes fail closed. Paper and shadow PnL are not pooled.

QA: 163 analyzer/replay/fill/evaluator/scorecard tests passed. Combined with the
isolated batch core, 185 passed and one real-symlink fixture was skipped because
this Windows host lacks creation privileges; the deterministic reparse-point
fixture passed. The reordered-input test intentionally expects a changed exact
input hash while verifying unchanged findings. Whitespace checks passed.

The normal analyzer still needs a current acknowledged mirror and validated
research-model contexts. Runtime publication and desktop/mobile rendering of
these new report fields remain OPEN; no new profitable policy is asserted.
