# Vercel Ignore-Build Probe

This file exists only to verify that Vercel's commandForIgnoringBuildStep
correctly skips builds for commits that touch no deployable code paths.

It will be removed after verification.
