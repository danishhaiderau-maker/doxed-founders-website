---
description: Global showcase bot — edit in monorepo; no blunt sync from local lab or research repo
globs: services/btc-conservative-agent/**,services/btc-signal-engine/**,scripts/patch-btc-bot-production.mjs,scripts/sync-*-btc*.mjs
alwaysApply: false
---

# Global showcase bot (canonical in-repo)

## Source of truth

Edit **`services/btc-conservative-agent/`** directly in this monorepo.

Do **not** blunt-sync from:
- `C:/Users/user/Desktop/Final Bots/bybit_bot.py` (local lab `:7800`)
- `bybit-15m-research-bot` GitHub repo

See `config/bot-architecture.lock.json` and `.cursor/rules/no-blunt-bot-sync.mdc`.

## Runtime

- Home showcase: `:7002` → `https://bot.doxxedcrypto.digital`
- `scripts/patch-btc-bot-production.mjs`: pause/resume, health, credentials guard, relay webhooks
- `showcase_ui.py`: sync status / fresh start APIs only

## Rule of thumb

Platform wiring (relay, Admin credentials, tunnel) = monorepo additions.

Signal + execution behavior = edited here, committed here — not mirrored from legacy bots.
