# Bybit (required at startup — research mode uses public WS + optional private balance check)
BYBIT_API_KEY=
BYBIT_SECRET=

# DeepSeek AI reviewer (optional — bot runs rule-based fallback without it)
DEEPSEEK_API_KEY=

# Flask dashboard (Agent Hub reads GET /api/state from this port)
PORT=5000
