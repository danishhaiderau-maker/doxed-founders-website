#!/usr/bin/env node
/**
 * Retired compatibility command.
 *
 * It previously decrypted production secrets into a desktop environment file
 * and instructed users to launch bot.py locally. That would create a second AI
 * owner, so the command is now deliberately inert.
 */
console.log('RETIRED: desktop strategy environment export is disabled.');
console.log('Fly.io doxed-btc-bot is the sole AI and trading owner.');
console.log('Desktop recovery starts only the :7002 Fly proxy and :9001 analyzer mirror.');
