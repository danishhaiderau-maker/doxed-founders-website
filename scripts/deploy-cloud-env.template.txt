# Copy each block into the matching dashboard (Railway / Vercel).
# Replace placeholders after deploy.

# ─── RAILWAY (API service → Variables) ───
# Use service: doxed-founders-website (NOT @dcf/web — web is on Vercel)
DATABASE_URL=YOUR_NEON_DATABASE_URL
JWT_SECRET=YOUR_JWT_SECRET
PRISMA_DB_PUSH=true
PRISMA_SCHEMA=prisma/schema.prisma
NODE_ENV=production
CORS_ORIGINS=https://doxxedcrypto.digital,https://www.doxxedcrypto.digital
WEB_APP_URL=https://doxxedcrypto.digital
API_URL=https://doxed-founders-website-production.up.railway.app

# GitHub OAuth — Founder OS one-click connect (create OAuth App at github.com/settings/developers)
GITHUB_CLIENT_ID=YOUR_GITHUB_OAUTH_CLIENT_ID
GITHUB_CLIENT_SECRET=YOUR_GITHUB_OAUTH_CLIENT_SECRET
GITHUB_OAUTH_CALLBACK_URL=https://doxed-founders-website-production.up.railway.app/api/auth/github/callback

# GitHub push webhook — instant commit sync (optional; 15-min poll remains as fallback)
GITHUB_WEBHOOK_SECRET=YOUR_GITHUB_WEBHOOK_SECRET
# Webhook URL: https://doxed-founders-website-production.up.railway.app/api/founder-os/webhooks/github

# Disable background poll if webhook-only: DISABLE_GITHUB_AUTO_SYNC=1

# ─── VERCEL (Project → Settings → Environment Variables) ───
NEXT_PUBLIC_API_URL=YOUR_RAILWAY_URL
API_URL=YOUR_RAILWAY_URL
NEXTAUTH_URL=YOUR_VERCEL_URL
NEXTAUTH_SECRET=YOUR_NEXTAUTH_SECRET
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET

# ─── GOOGLE CLOUD (OAuth client → add URIs) ───
# Authorized JavaScript origin: YOUR_VERCEL_URL
# Authorized redirect URI: YOUR_VERCEL_URL/api/auth/callback/google

# ─── AFTER FIRST SUCCESSFUL RAILWAY DEPLOY ───
# Delete PRISMA_DB_PUSH from Railway variables
